# MANAS — Deployment Guide

## Backend → Render

### One-time setup

1. **Push to GitHub**
   ```bash
   git add .
   git commit -m "Initial MANAS build"
   git push origin main
   ```

2. **Create Render Blueprint**
   - Go to [render.com](https://render.com) → New → Blueprint
   - Connect your GitHub repo
   - Render reads `render.yaml` automatically
   - Two resources will be created: `manas-db` (PostgreSQL) and `manas-api` (Node web service)

3. **Wait for first deploy**
   - The build command installs TypeScript build dependencies, generates Prisma Client, builds the backend, and runs `prisma db push`.
   - First deploy takes ~3-5 minutes.
   - The Render service root now returns safe API metadata as JSON. Use `/health` for Render health checks and uptime probes.

### Render web service settings

Use these exact values for the `manas-api` web service:

| Setting | Value |
|---|---|
| Root Directory | `backend` |
| Build Command | `npm ci --include=dev && npx prisma generate && npm run build && npx prisma db push` |
| Start Command | `npm run start` |

Set these environment variables:

| Variable | Value |
|---|---|
| `DATABASE_URL` | Render PostgreSQL connection string |
| `JWT_SECRET` | A long random secret |
| `NODE_ENV` | `production` |
| `NODE_VERSION` | `20` |
| `EMAIL_USER` | Sender email account username |
| `EMAIL_PASS` | Sender email app password |
| `EMAIL_FROM` | `MANAS <no-reply@manas.app>` or your sender address |
| `OTP_EXPIRY_MINUTES` | `10` |

If you use an existing manually-created Render service, changing `render.yaml` alone may not update the dashboard command. In that case, update the Render dashboard Build Command manually to match the value above.

This repository does not currently include Prisma migrations, so the first Render deploy uses `npx prisma db push`. Production should later move to checked-in Prisma migrations and `prisma migrate deploy` once migrations exist.

4. **Seed the database** (run once)
   - Open Render dashboard → `manas-api` → Shell
   ```bash
   npm run db:seed
   ```
   Or run schema sync and seed together:
   ```bash
   npm run db:setup
   ```

   The seed is written to be safe to rerun, but do not add it to the Render build command unless you intentionally want every deploy to reconcile seed data. If `/categories` returns `[]`, the mobile app can open but the product will look empty because the production database has not been seeded.

5. **Copy the live URL**
   - Your API lives at: `https://manas-api-dlj7.onrender.com`
   - Browser root: `https://manas-api-dlj7.onrender.com/`
   - Health check: `curl https://manas-api-dlj7.onrender.com/health`
   - Full production smoke after deploy and seed:
   ```bash
   cd backend
   API_URL=https://manas-api-dlj7.onrender.com npm run smoke:prod
   ```

6. **Update mobile app**
   - Set `EXPO_PUBLIC_API_URL` in `mobile/.env.production` to the Render URL
   - The `eas.json` preview/production profiles already include this env var

### Environment variables (managed by Render)

| Variable | How it's set |
|---|---|
| `DATABASE_URL` | Auto-linked from `manas-db` |
| `JWT_SECRET` | Auto-generated by Render (`generateValue: true`) |
| `NODE_ENV` | `production` |
| `NODE_VERSION` | `20` |
| `PORT` | Render injects this automatically; do not hardcode it in `render.yaml` |
| `FRONTEND_URL` | Update to your frontend domain |
| `EMAIL_USER` | Add manually in Render; backend-only |
| `EMAIL_PASS` | Add manually in Render; backend-only app password |
| `EMAIL_FROM` | Optional sender display address |
| `OTP_EXPIRY_MINUTES` | `10` |
| `GOOGLE_CLIENT_ID` | Add manually when implementing Google OAuth |

### Free tier notes

- **Cold starts**: The free plan spins down after 15 minutes of inactivity. First request after spin-down takes 30–60 seconds. This is expected.
- **Database**: Render's free PostgreSQL expires after 90 days — upgrade to a paid plan or export data before expiry.
- **Logs**: Render dashboard → `manas-api` → Logs
- **Node versions**: Render runs the backend with Node 20. Mobile Expo SDK 56 local builds use the repo `.nvmrc` and require Node 22.13.1 or newer.

### Re-deploying

Every push to `main` triggers an automatic redeploy. To manually deploy:
```
Render dashboard → manas-api → Manual Deploy → Deploy latest commit
```

### Adding migrations after schema changes

```bash
# Local
cd backend
npx prisma migrate dev --name <migration-name>
git add prisma/migrations
git commit -m "Add migration: <name>"
git push
```

After migrations are checked in, switch the Render build command from `npx prisma db push` to `npx prisma migrate deploy`.
